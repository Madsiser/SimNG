package pl.simNG;

/**
 * Enum reprezentujący strony konfliktu w symulacji.
 * BLUFORCE - reprezentuje siły przyjazne (Blue Force).
 * REDFORCE - reprezentuje siły wroga (Red Force).
 */
public enum SimForceType {
    BLUFORCE,
    REDFORCE
}

